# Modo "emulator" para Vite — usar con: npm run dev:emulator
# El SDK requiere los valores de Firebase pero el emulador los ignora; quedan
# tomados de tu .env.local. Solo necesitamos forzar el toggle:
VITE_USE_EMULATOR=true
